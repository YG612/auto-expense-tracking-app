# 更新记录

## 1.0.7（build 8）— 轻量流式 ASR 实验候选，尚未生产发布

### Android App 自持流式语音

- 新增显式 Android Internal `streamingAsr` 实验轨道。只有该轨道内置 sherpa-ncnn 与约 25 MB 的 Zipformer 普通话模型；普通 Internal、Production、Debug 与 iOS 均不内置模型。
- 实验轨道由 App 自持 AudioRecord，以 16 kHz 单声道 PCM16 在原生层流式解码，PCM 不跨 React Native、不保存也不上传。
- 模型端点检测关闭；partial 只用于屏幕转写，只有用户点按“说完了”才停止采集、排空 decoder 并产生 final。取消、切后台和 30 秒安全超时不会产生 final。
- 系统兼容包仍可由用户显式选择设备系统语音；实验包运行时或模型损坏时不得静默切换系统端点。

### 状态与发布边界

- 原生公共层、普通 Internal Kotlin/JVM、显式 `streamingAsr` runtime/model 供应链和最终 APK 静态门禁已通过。
- 普通 arm64 Internal APK 为 26,591,742 bytes；流式离线 APK 为 56,450,596 bytes，实际增量 28.48 MiB。二者均不声明 `INTERNET`，流式包仅包含 arm64 运行库。
- 已移除被轻量流式方案取代的 SenseVoice 模型、运行库、构建开关和脚本，避免仓库同时维护两条离线推理链。
- 尚未完成 USB 真机、普通话准确率、口音、噪声、内存、延迟、功耗和长时间稳定性验证，不能标记为发布就绪。

### 发布身份

- 版本统一为 Android/iOS/package `1.0.7` / build `8`，并保留 `1.0.6` / build `7` 发布历史。

## 1.0.6（build 7）— 内测候选，尚未生产发布

### 语音结果消费边界

- 旧语音结果按会话代际隔离，并且每个结果只允许消费一次，避免过期回调进入新的记账会话。
- `VOICE` 提交增加 durable receipt，跨重试和恢复过程防止同一语音结果被重放入账。
- 系统语音 Provider（包括 MiBrain）仍可能自行自动断句或提前结束。真正由用户手动停止采集需要 app-owned offline ASR；本版本不宣称已解决 MiBrain 自动停止。

### 发布身份

- 版本统一为 Android/iOS/package `1.0.6` / build `7`，并保留 `1.0.5` / build `6` 发布历史。

## 1.0.5（build 6）— 内测候选，尚未生产发布

### 规则建议与确认体验

- 个人规则明确提供分类或账户时，不再把它误写成“最近账户推断”的阻断歧义；信息完整的候选可在卡片外层一次点按“确认入账”，仍保留“检查并编辑”，不会静默自动入账。
- 仅使用最近账户补全时改为非阻断提示，用户仍需明确确认；缺字段、低置信度或金额/类型等关键风险继续强制编辑或进入待处理，安全边界不放宽。
- 无个人规则时，“坐车、乘车、车费”等口语先归到通用“交通”一级分类；个人规则仍可覆盖为“公交”等更细分类。

### 安装包与离线语音边界

- 默认 Android Internal 保持轻量且不内置语音模型或 sherpa-onnx；普通系统语音仍可能受设备 Provider、本地中文能力和网络条件影响。
- 包含 SenseVoice 的 arm64 离线实验 APK 约 295 MB，只保留为受控 PoC，不作为默认安装包或生产方案。更小模型仍待权重许可证核验和同一语料 A/B，当前版本不宣称离线语音问题已解决。

### 发布身份

- 版本统一为 Android/iOS/package `1.0.5` / build `6`，并保留 `1.0.4` / build `5` 发布历史。

## 1.0.4（build 5）— 内测候选，尚未生产发布

### 交易解析

- 修复“数量 × 单价”把单价误记为总金额的问题，并补充“沙县小吃”等餐饮商户与分类识别；表达中的总价与计算结果冲突时安全转入复核，不猜测入账。

### 记账精简

- 手动记账和流水筛选的首层交易类型收敛为“支出、收入、转账”；退款、报销、借还款和余额调整进入“更多类型”。底层 10 种账务语义仍完整保留，历史账和统计口径不变。
- 分类首屏只展示最多 8 个常用一级分类，完整二级分类和自定义分类仍可搜索；商户、项目、标签和备注折叠到“更多信息”。新账不再擅自默认分类，编辑旧账仍保留原分类引用。

### 语音会话

- 系统/OEM Provider 自动断句不再直接提交记账。转写片段可以去重拼接并由用户选择“继续说”，只有明确点按“说完了”或“使用这段文字”才进入交易解析。
- 原生与 TypeScript 会话补齐采集所有权、端点所有权和结束原因；外部系统窗口仍是用户显式选择的“可能联网”兼容回退，不会静默替代本地路径。

### Android 离线 ASR 实验包

- 增加可选的 Android Internal `offline-asr` 实验构建：只在显式启用时内置锁定版本的 sherpa-onnx 与 SenseVoiceSmall int8，并由 App 自持麦克风 PCM 采集和本机解码。
- 实验包固定为 arm64-v8a；构建前校验运行时、模型、许可证、署名和 SHA-256，构建后校验权限、ABI 与必需资产。它不声明 `INTERNET`，不上传录音或转写。
- 普通 Internal、Production、Debug 和 iOS 仍不包含模型或 sherpa-onnx 运行时；系统语音继续作为逐次明确同意的兼容回退。
- 该实验构建只证明可集成和可验证，不代表普通话、金额、口音、噪声、内存或延迟门槛已经通过；iOS 离线识别尚未实现或验证。

### 发布身份

- 版本统一为 Android/iOS/package `1.0.4` / build `5`，并保留 `1.0.3` / build `4` 发布历史。

## 1.0.3（build 4）— 内测候选，尚未生产发布

### 记账交互

- 智能记账收敛为“核对账单 → 一次确认入账”：解析不是第二次确认，用户只在看到完整账单后执行一次财务确认。
- 结构完整且没有金额、类型、账户、转账或汇总风险的中等置信建议可以由用户明确确认；关键歧义仍必须编辑或存入待处理，持久化安全边界没有放宽。
- 确认成功后进入独立完成状态，已保存卡片立即从当前会话消失，不再与之前的语音能力提示同屏；重复点击仍由会话和写入幂等保护。

### 语音兼容

- “本机无离线普通话模型”改为中性能力提示，不再作为持续红色故障占据主输入流程；文字输入始终可用。
- 所有可能联网的系统语音入口和重试操作都明确标注“可能联网”，未经用户当次明确选择不会切换到 Google 或其他外部 Provider。
- Android 在用户同意可能联网、权限有效且服务健康时优先使用 App 内可控的系统识别会话，保留“说完了”和取消；只有运行失败或超时才兼容降级到外部系统语音界面。
- 本版本没有内置语音模型，也没有给 Internal/Production 增加 `INTERNET` 权限。外部系统语音仍只是显式兼容回退，其网络、端点检测和界面行为由设备 Provider 控制。

### 工程与后续门禁

- 版本统一为 Android/iOS/package `1.0.3` / build `4`，并保留 `1.0.2` / build `3` 发布历史。
- 增加智能记账一次确认、中等置信安全边界、语音能力提示和 Android 路由降级的回归覆盖。
- 新增 `docs/EMBEDDED_ASR_POC.md`：后续只通过隔离 PoC 评估可选离线普通话语音包，不把未经测量的 200 MB 级模型直接塞进基础 APK。

### 仍需真机确认

- 新 APK 仍需在 Redmi/HyperOS、ColorOS/OxygenOS、MagicOS、Pixel/AOSP 和 Samsung/其他 Provider 组合上回归；品牌不能替代能力矩阵。
- 外部系统语音可能因设备没有中文模型、Google 不可联网或 Provider 自行提前结束而失败。失败时必须保留已有文字并允许继续输入，不能宣传为 App 自有离线识别。
- iOS 仍需在 macOS 完成依赖锁审查、Simulator/Archive 和真机语音验证。

## 1.0.2（build 3）— 内测候选，尚未生产发布

### 修复

- 语音能力按权限、provider、本地中文模型状态和用户联网同意分别建模；Android 13+ 支持能力检查与模型下载，ColorOS 等 OEM 可显式降级到系统语音界面。
- 收入不再默认误判为支出，补齐工资、奖金、兼职、理财收益、出售等收入语义，并在写入前校验交易类型与分类方向。
- 中文交易解析改为“数值角色 → 交易事件 → 类型/分类”的安全管线：数量、时间、编号、外币和非法小数不再冒充人民币金额；无标点多笔、总价对账、否定/失败/计划语义和未定价事件均会正确拆分或安全降级。`今天下午去商场买两瓶牛奶然后花了25元` 现在只生成一笔 25 元的食品杂货支出。
- 智能记账改为独立会话状态机；直接确认、暂存、编辑确认和失败恢复使用同一持久化路径，成功后只移除已保存卡片。
- 智能记账页收敛为一个输入区和一个情境主操作；识别后优先展示精简确认卡，技术细节按需展开，未保存结果只能通过明确的“放弃并重新输入”操作清除。
- 确认资格由统一规则决定：只有高置信、无缺失、无歧义、无替代分类的候选可以直接入账；其余候选必须编辑或进入待确认箱，页面和数据库写入边界使用同一口径。
- 从系统权限或麦克风设置返回后只做被动能力复检，不会自动录音或联网；离线语音包下载期间保持准备状态，并提供显式就绪检测，避免页面陷入无恢复的等待。
- 交易新增 revision 乐观锁；旧页面不能覆盖新修改或复活已删除记录。

### 安全与隐私

- Debug、Internal、Production 三个 Android 身份隔离；Production Release 不再回退公开 Debug key，缺少外部签名配置时构建失败。
- Production/Internal Android 不持有 INTERNET；仅 Metro Debug 保留。
- Android 云备份与设备迁移排除财务数据；iOS SQLite 主库、WAL、SHM 排除系统备份并启用首次解锁文件保护。
- 设置可关闭原始文字留存；关闭时事务性清除历史交易/纠错原文，后续所有写入口继续执行该策略。
- UI 只展示审核过的安全文案和稳定错误码，不展示 SQL、路径、原始异常或账目正文。

### 工程门禁

- 版本统一为 Android/iOS/package `1.0.2` / build `3`。
- Gradle Wrapper 增加官方分发 SHA-256；CI 使用 frozen lock、身份攻击测试、迁移不可变测试、JS 质量门、Android JVM tests 与隔离 Internal 构建。
- 增加安全的 Internal USB 安装器和脱敏真机回归工具；安装器不卸载或清数据，回归证据固定写入 D 盘且不采集序列号、账本、录音或全量日志。
- v1–v5 migration 由 SHA-256 清单保护，只允许追加新迁移；v5 持久化待确认风险标记，历史待确认记录默认要求人工复核；禁止改写历史或 destructive down migration。

### 发布前仍需完成

- 已在 D 盘 Android SDK/Gradle 环境完成 arm64 Internal 原生测试与构建；`app-internal.apk` 已通过包身份、权限、16 KB 对齐、APK v2 签名、ABI 和 Hermes Bundle 一致性验证。
- Node 24 全量质量门为 31 个 Jest 套件、229 项测试全部通过，TypeScript、ESLint、Prettier、发布身份与迁移不可变门禁全部通过；PowerShell 安装器另有 11 项安全契约检查。
- 仍需在 Redmi、MagicOS、ColorOS 及其他能力组合的真机上回归本地识别、系统语音输入和离线降级；并在 macOS 完成 iOS Simulator/Archive 与真机验证。
- PRD 第一版仍缺隐私锁、CSV、主动备份/恢复、删除全部数据等发布必需项；阶段完成不等于生产就绪。

历史上以公开 Debug key 签名的 `com.qingjiai` 测试包不能原地升级为正式签名。测试人员应改装 `com.qingjiai.internal`；生产上线前按发布手册处理旧测试身份。
